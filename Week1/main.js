//console.log("hi <33")
//js is an interpreted language while cpp is a compiled lang
//interpreted lang go line by line while executing, can run partially until error
let firstName="shivam";
let age=21;
let isMarried=false;

if(isMarried)
{
    console.log(`${firstName} is married`);
}
else{
    console.log(`${firstName} is not married`)
}